# Optional. Leave unset during local dev — Vite proxies /api to localhost:8080 (works with dev tunnels).
# Set this in production to your deployed backend URL, e.g. https://api.yoursite.com
# VITE_API_BASE_URL=https://your-backend-url.com
